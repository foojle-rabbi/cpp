#include<iostream>
using namespace std;

int main()
{
    /*
    Accha goto koyekta program e amra template somporke janchilam. ar eibar amra StL shomporke janbo. stl menas: standard
    templaete library. orthat ei stl hocche onek gula inbuild function er somosthi. accha jemon, link list, vecotr, map
    maping, hash, hasing etc. orthat onke inbuilt subidha paoya jay jeigula use kore programming onek easy kora jay. ar 
    harry miar mote, stl competetive programming e onek use. 

    ar tahcra stl use kolre onek time er kajo sohoje kora jay. accha jaak faul kotha koiya program voar kono dorkar nia, ekhon
    kota hocche stl er kichu components ache, seigula hoilo:
        1. container
        2. algorithms
        3. iterators
    1. container:
        cotainer kore ki data sotre kore. jei kono dhoroner data hoite pare int flat, char, doulbe etc;
    2. Algorithms:
        algorithms soting, merging, size, serching etc kaj kore.
    3. Iterators:
        -> iterators hocche ekta object which point to an element in a container.
        -> Handaled like a pointer. 
        -> connect algo with container

    accha eigula gelo tader theroy part. etto kotha baad stl hoitehce, tui je data stucture and algorithms porchos na tader 
    e kichu algorithms ei stl e automatic thake(automatic thake bolte header file library include kora lage) arki. abar tader
    moddhe kichu ache container jemon vecotr, map, multimap, set multiset etc; haa eita true je tui chile eigular moto tor
    nijer iccha moto algorithms implement korte parbi. but stl use korle tor kaaj aro easy ar kom time lagbo..
    */
    return 0;
}