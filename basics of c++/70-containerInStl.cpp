#include<iostream>
using namespace std;

int main()
{
    /*
    accha stl hoitehce: container + alogrithms + iterators er misron.

    1. container -> which stores data; 
    2. algorithms -> procedure to process data;
    3. iterators -> an object which point to an element of a container;
    
    1. Container: containers are 3 types,
        1.1. sequence container
        1.2. associative container
        1.3. derived container

        1.1. sequence container: linerar fashion
            a. vector
            b. list
            c. dequeue

        1.2 associatvie container: direct access
            a. set
            b. multiset
            c. map
            d. multimap
        1.3. Derived container: real world
            a. stack 
            b. queue
            c. priority queue
    --> zaak bohut lekhchi eto kahini nai. protita jinisher e ekta plus point ache abar prtita jinisher abar drawback o ahce
    amra amader uses ba demand buijha ekektare ekeshomoy use kormu.
    */
    return 0;
}