#include<iostream>
using namespace std;

int main()
{
    int arr1[] = {1, 2, 4, 6};
    int arr2[] = {2, 4};
    int n = sizeof(arr1) / sizeof(arr1[0]);
    int m = sizeof(arr2) / sizeof(arr2[0]);
    return 0;
}