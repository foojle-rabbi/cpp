#include<iostream>
using namespace std;

int main()
{
    /*
    longest common prefix mane hocche amake koyekta word deoya thakbe tar moddhe koto ta word porjonto common ar ki ki common word 
    sheta return korte hobe. suppose, you have a string of word: {"coding", "coder", "codbro", "cobald"}; toh ekhon eikhane shob gulate
    prefix mane toh ager toh ager ktota porjonto same ache? sheta hoccche only "co" porjonto tai amar ans o hobe "co";

    besh ei tuku e. tobe eita solve korar 2 way ache. one is iteration method and the other is tries methob. iterator e amare 2 ta loop 
    maintiain korte hobe. ar tries e amar extra ekta variable declare korte hobe to count the child nodes. if there is more then one child
    of the root node that this part is the ans. 

    just return the ans and you done. 
    */
    return 0;
}