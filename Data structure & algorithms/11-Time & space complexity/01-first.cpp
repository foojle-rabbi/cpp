#include<iostream>
using namespace std;

int main()
{
    /*
    Time complexity mane ekta program/code execute ba cholte kototuku time lagche tar shomoy kei time complexity bole;
    ar kono ekta program cholar shomoy kotouku space ba memory niche tare space complexity bole;

    time & space complexity re muloto 3 ta jinish dia prokash kore; 
        1. Bit o notion -> for upper bound
        2. Theta -> For average case
        3. Omega -> For lower bound
    -> constant time -> O(1)
    -> Linear time -> O(n)
    -> Logarithim time -> O(logn)
    -> Quadric time -> O(n^2)
    -> cubic time -> O(n^3)

    ^ O(N!)       
    ^ O(2^N)
    ^ O(N^3)
    ^ O(N^2)
    ^ O(nlogn)
    ^ O(n)
    ^ O(logn)
    ^ O(1)
    ^  
    | orthat eitar joto upore jaiteche time complexity toto bartehce;

    Second space comeplexity kebol array create kortei khay; mane normal array er size declare kore array declare korle constant
    space e khay; kintu size jodi na bole dey mane dynamically create korle Big O(n); space khay; ar emnite nomal variable
    declare korle temon space khay na; 
    
    */
    return 0;
}